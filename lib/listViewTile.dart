import 'package:flutter/material.dart';

class ListTileView extends StatelessWidget {
  ListTileView({@required this.title});
  final Text title;
  @override
  Widget build(BuildContext context) {
    return Container(
      //
    );
  }
}
